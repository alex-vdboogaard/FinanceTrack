import Modal from "../../components/modal/Modal";

export default function NewReview({budget}) {
    return (
        <Modal>
            <h2>New Monthly review</h2>
            {/* Month, year, income, expenses, invested, saved */}
        </Modal>
    )
}